\documentclass{article}
\begin{document}

Here is a paragraph to test basic typesetting and the line breaking algorithms. The engine should handle normal text flowing across the page, calculating the stretch and shrink of glue between words.

We also need to test different types of hyphens. A single hyphen is used for compound words like well-known or state-of-the-art. An en-dash is created with two hyphens and denotes ranges, such as pages 42--56 or the years 1990--2000. An em-dash is created with three hyphens and is used to denote a pause in thought---just like this one. 

Now, let us test some mathematical expressions. Based on the parser bindings, display math is triggered using specific bracket tokens. Here is an equation utilizing some of the registered Greek unicodes:

\[
\alpha + \xi = \pi
\]

Here is another equation testing the infinity symbol and standard mathematical layout:

\[
\int_{0}^{\infty} f(x) dx = \omega
\]

This concludes the basic parsing and layout test for the engine.

\end{document}
